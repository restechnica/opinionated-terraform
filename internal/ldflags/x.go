package ldflags

// Variables set at build time: -ldflags "-X github.com/restechnica/opinionated-terraform/internal/ldflags.<var>=<value>"

var (
	// Version is the release version of otf.
	Version = "dev"
)
