package cli

const (
	// DefaultBackendsDir is the directory containing backend configuration files.
	DefaultBackendsDir = "backends"

	// DefaultVariablesDir is the directory containing variable files.
	DefaultVariablesDir = "variables"

	// DefaultEnvFile is the filename used to track the current environment inside .terraform/.
	DefaultEnvFile = ".otf"
)
